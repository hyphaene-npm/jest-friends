import React from 'react';
import PropTypes from 'prop-types';

export const ExampleClass = ({ blabla }) => <div>bliii</div>;

ExampleClass.propTypes = {
	blabla: PropTypes.string,
};

export default ExampleClass;
